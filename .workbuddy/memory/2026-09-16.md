
## 2026-09-16 界面重构计划
- 应用户要求产出《Agent化界面重构_实施计划.md》（已存桌面 C:\Users\zhy23\Desktop\），定位 Agent 自动标注软件、Codex 风格。
- 先用 AskUserQuestion 确认 4 个决策：工作台删除后保留只读预览+对话修正；对话自动建项目（去表单预填与示例引导）；训练/流程对话化=对话卡片+侧栏任务看板；数据管理收进项目概览+对话。
- 已核实关键落点：Sidebar.tsx mainEntries 7 项；Projects.tsx:39 无条件「还没有 API Key」横幅（G6 要修）；demo.ts project.example 预置城市场景示例；新建项目表单预填类别。
- 计划中唯一引擎侧改动：放开训练触发命令的 agent 白名单（保持训练冻结约束），建议单独 PR。
- 同轮完成项目 MEMORY.md 超限压缩（约 4.3K→约 2.9K 字符）。
## 2026-09-16 追加：停止实施，改为修订计划文档
- 用户澄清：本轮不要开始写代码，只要修改桌面上的《Agent化界面重构_实施计划.md》。
- 我曾先动手改了 renderer\src\Projects.tsx（API 横幅状态驱动），已 git checkout 回滚，工作区恢复原状。
- 计划新增 §8.1「提交节奏」：每完成一个小步骤立即提交 GitHub、中文简要备注（[界面重构 N] 描述），含 17 个步骤的提交点表格（文件范围 + 提交信息示例）与 5 条规则；§10 增补第 5 条协作约定。
- 项目 MEMORY.md 完成压缩重写，并把「每小步一提交、中文备注」写入第 6 节长期约定。
- 提醒：仓库现有 desktop\main.ts、scripts\desktop-dev.mjs 两处改动不属于本次界面重构，未提交也未改动。
## 2026-09-16 追加：实施计划步骤 10–17（本次会话）
- 起点：计划 1–9 与此前若干界面提交已完成；本次从步骤 10 接着做，全部按「一小步一提交 + 中文备注」推送。
- 步骤 10 `[界面重构 10]`：训练触发/查询进 agent 白名单（desktop/validation.ts agentCommands + assertAgentCommand 只允许 `training.dataset.create` 的 source=version）；新增 `agent/training-tools.ts`（8 个工具，只读摘要不返回清单/日志/路径）与测试。
- 步骤 11 `[界面重构 11]`：补齐计划步骤 9 缺失的内容——对话内工具步骤流（AgentActivity.tsx，模块级按会话累积）、先看方案的确认卡片、训练/流程任务卡片（TaskCards.tsx，4 秒轮询 + 详情复用 TrainingJobDetail.tsx）；SYSTEM 提示补训练约束。
- 步骤 12 `[界面重构 12]`：任务页扩到六页签（自动流程/标注/素材/训练/导出/轨迹），新增训练与导出只读看板、侧栏「任务」进行中徽标（activeTasks.ts）、任务→来源会话映射（localStorage taskOrigins）。
- 步骤 13 `[界面重构 13]`：新增 ProjectOverview.tsx（素材统计/数据集版本/导出记录 + 只读抽查），不占主导航，侧栏项目行与快速跳转进入。
- 步骤 14 `[界面重构 14]`：删除 Workbench / Workflow / Training / Resources / Projects 与 7 个只被它们引用的模块（-1449 行）；能力入口改落项目概览（素材操作、点位模板、资源应用）与对话引用面板（新建人工参考）；ui-check 与 smoke 断言同步改写。
- 步骤 15 `[界面重构 15]`：Models.tsx → AiSettings.tsx，作为设置「软件 AI 配置」区块；Page 类型去掉 models、navLegacy 清空；agent 侧与 README 的「模型中心」文案改指设置。
- 步骤 16 `[界面重构 16]`：发布验收脚本 release5/6a/6b/7a/7b 的界面步骤改到项目概览与任务看板（引擎级断言不动），esbuild 打包校验通过；--ui 与 --training-ui 源码态检查通过。
- 步骤 17（无源码改动、无提交）：pack:dir 构建目录包（需先设 AUTOLABEL_MEDIA_TOOLS_DIR=build/media-tools），包内 jar SHA256 与仓库一致；`--packaged` 冒烟与 `--packaged --ui` 均通过（此前记忆里「本沙箱超时未闭环」已不成立）；用 --dir 未安装，因此无需卸载。
- 未完成/待决：`renderer/tests/desktop-manual-check.ts` 等旧画布验收脚本仍失效，需要按对话式界面重写或退役。

## 界面审核（2026-09-16 下午）
- 用 CDP 截图法审界面：electron 加 --remote-debugging-port=9333 加 --no-sandbox --in-process-gpu --disable-gpu，node 22 原生 WebSocket 连 /json/list 的 page target，Emulation.setDeviceMetricsOverride 1440x900 后 Page.captureScreenshot；驱动切页用 Runtime.evaluate 点 DOM（改 location.hash 不触发 React 路由）。脚本曾放 tmp/ui-audit/shoot.mjs，已删，方法记于此。
- 测试实例数据：复制真实 dataDir（APPDATA 下 自动标注小助手/desktop-settings.json 里的 dataDir）到 tmp 副本并写测试 desktop-settings.json；真实库当前无项目，欢迎页即真实空态。
- 审核要点结论（供后续改）：帮助弹窗仍是已删工作台的画布快捷键文案（App.tsx help modal，过时误导）；chat 页顶栏不显示当前项目名；快速跳转只搜 4 个页面价值低；助手消息纯文本渲染不支持 Markdown；设置 12 区块页签过载；侧栏底部状态行文字截断。
- 视频 C:\Users\zhy23\Desktop\video 3.mp4：22.24s 3840x2160 60fps HEVC 166MB，柜子+手办（Bocchi 手办）手持运镜，建议走欢迎页拖入视频→抽帧→对话发起检测标注。