- Communicates in Chinese and expects Chinese-language replies, UI copy, and document text (Simplified Chinese). Confidence: 0.75
- Prefers the assistant to work autonomously on multi-step/long-running work: gives terse continuation cues (e.g. "继续", "请你开始收尾") or bare imperatives (e.g. "请你启动软件界面") and delegates prioritization, sequencing, and the execution itself instead of specifying them — resolve the how (read package.json/README, install deps, launch) on his behalf rather than replying with instructions — expects the assistant to build its own task list and carry through the whole cleanup without further prompting. Confidence: 0.75
- Wants commits/pushes documented with brief Chinese commit messages (expects git commits to GitHub to carry a concise Simplified-Chinese summary rather than a long or English-only message). Confidence: 0.7
- Uses git/GitHub as the delivery step and expects the assistant to run the git workflow itself (e.g. "推送至GitHub"): inspect status/tracking first, then commit and push finished work rather than leaving changes only in the working tree. Confidence: 0.6
- Drives work from written implementation-plan documents (kept as .md files, e.g. on the desktop and mirrored in a repo plans folder) rather than restating scope in chat; expects the assistant to locate and read those plans and pick up the remaining/unfinished items. Confidence: 0.75
- Steers product structure with one terse, high-level statement (e.g. "对话是要集成在项目内的") rather than a spec, and expects it applied as a binding constraint across every affected surface (navigation, sidebar grouping, entry flows), not just the screen under discussion — confirm the intended structure via concrete options when it is ambiguous instead of guessing. Confidence: 0.5
- Reports UI/visual bugs as a screenshot plus a few words (e.g. a blank window + "界面什么都没有") instead of written reproduction steps — expects the assistant to diagnose from the image and verify the fix visually (screenshot/DOM probe) before declaring it fixed. Confidence: 0.5
- On resuming a written plan, expects the assistant not to be blocked by pre-existing/unrelated working-tree changes: says they can be ignored and to treat the code as unchanged ("已有的改动不用理会，默认全部未改动") and to just continue from the plan — don't pause to reconcile, revert, or fold those unrelated diffs into the work. Confidence: 0.6
- Expects the assistant to clean up after its own testing: any test conversations/sessions it started, isolated user-data directories, probe scripts, screenshots, and temp artifacts must be deleted once verification is done (while leaving pre-existing files he did not create alone). Confidence: 0.65
- Wants UI lists that can grow unbounded (projects, chat sessions) visually capped by default — show only the most recent few and fold the rest into an expandable "还有 N 条 / 全部" row rather than rendering everything, keeping the underlying data intact. Confidence: 0.55
- Asks for plan-completion audits: when pointing at a plan document (e.g. "任务完成了吗"), expects the assistant to check each acceptance item against the actual code/build artifacts/QA evidence and report honestly which items are still unfinished, rather than accepting a "done" claim. Confidence: 0.6
- Expects the chat input surface to accept content dropped directly onto it (files, images, videos) as a first-class input path, not only typed text or file-picker flows — states it as a hard requirement ("我要求对话能够直接拖入文件，图片，视频等内容") rather than a nice-to-have. Confidence: 0.5
- Wants plan work shipped incrementally: each plan step becomes its own commit, tagged in Chinese with a bracketed phase/step label (e.g. "[界面重构 10]"), and pushed to GitHub right after that step is verified and before starting the next one — not batched into a single big commit or left unpushed until the end. Confidence: 0.5
- Prefers compact, collapsed controls over large inline ones: options such as model and thinking-depth should be a small one-line trigger that opens a selection popover on click, with the current choice echoed as small text (e.g. below the input) rather than rendered as big always-visible selects. Confidence: 0.6
- Wants the chat composer/input docked at the bottom of the page as the fixed anchor of every chat surface (welcome and session pages alike), with only the message area scrolling and the controls riding along with the input. Confidence: 0.55
- Prefers capabilities (task flows, task progress) surfaced inside the conversation instead of as separate navigation destinations — the primary nav should be reduced to a single "new chat" action with secondary pages demoted to low-key bottom entries. Confidence: 0.5
